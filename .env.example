# Copy to .env and adjust for your machine. Never commit .env.

# Kafka broker (master + workers)
KAFKA_BROKER=localhost:9092

# Redis (master only)
REDIS_HOST=localhost
REDIS_PORT=6379

# Node identity
INSTANCE_ID=master-1
WORKER_ID=worker-1
